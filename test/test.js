const assert = require('assert').strict;
const expect = require('chai').expect;

describe('While fetching a quote', () => {
	describe('', () => {
		it('should return -1 when value is not present', () => {
			expect([1, 2, 3].indexOf(4)).to.equal(-1);
		});
	});
});